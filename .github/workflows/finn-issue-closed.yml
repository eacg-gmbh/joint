name: "Finn: Issue Closed"

# Thin caller stub — logic lives in the reusable workflow in eacg-gmbh/team.
# See ADR-016.

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write # close-maintenance-source closes the source issue
  pull-requests: write # ...and the source Dependabot PR

jobs:
  agent:
    uses: eacg-gmbh/team/.github/workflows/finn-issue-closed-reusable.yml@main
    secrets: inherit
