name: Paul — PR Review

# Thin caller stub — logic lives in the reusable workflow in eacg-gmbh/team.
# See ADR-016.

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  agent:
    uses: eacg-gmbh/team/.github/workflows/paul-review-reusable.yml@main
    secrets: inherit
