name: "Finn: Ingest Issue"

# Thin caller stub — logic lives in the reusable workflow in eacg-gmbh/team.
# See ADR-016.

on:
  issues:
    types: [labeled]

jobs:
  agent:
    uses: eacg-gmbh/team/.github/workflows/finn-ingest-reusable.yml@main
    secrets: inherit
